let foo = 42;

console.log(typeof foo);

const name = "han";

let anything;
// 선언 후 할당을 하지 않으면 undefined가 할당됨

const hobbies = ["walking", "boooks"];

const address = {};
// 배열도 오브젝트의 일종이므로 type으로 검사하면 Object로 나옴
